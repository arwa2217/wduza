export const PermissionConstants = (t) => {
  return {
    VIEW: t("upload.modal:view"),
    DOWNLOAD: t("upload.modal:view.download"),
    NOT_ALLOWED: t("upload.modal:not.allowed"),
    VIEW_ENUM: t("upload.modal:ro"),
    DOWNLOAD_ENUM: t("upload.modal:dl"),
    NOT_ALLOWED_ENUM: t("upload.modal:np"),
  };
};
